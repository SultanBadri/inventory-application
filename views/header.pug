head
    body
      div.header
        a(href="/inventory" style="font-size: 1.6em") Fallout Weapons
        ul
          li
            a.category(href="/inventory") Home
          li.dropdown
            a Categories
            div.dropdown-menu
              each category in category_list
                a.category(href=category.url) #{category.name}